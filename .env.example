# Google OAuth configuration (server-side only)
# Copy this file to .env.local and fill with your real values.

GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_API_KEY=
GOOGLE_OAUTH_APP_ID=
GOOGLE_OAUTH_PROJECT_NUMBER=

# Must match exactly one Authorized redirect URI in Google Cloud Console.
# Example for production:
# GOOGLE_OAUTH_REDIRECT_URI=https://justcalendar.ai/api/auth/google/callback
GOOGLE_OAUTH_REDIRECT_URI=

# Where callback redirects after successful auth.
GOOGLE_OAUTH_POST_AUTH_REDIRECT=/
